import { createTheme } from '@material-ui/core/styles';

export const defaultTheme = createTheme({
  palette: {
    primary: {
      main: "#1565D8"
    },
    secondary: {
      main: "#696F79"
    },
  },
});
